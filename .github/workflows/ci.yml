name: CI with Pixi
on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test-pixi:
    name: Pixi on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v5
    
    - name: Setup Pixi
      uses: prefix-dev/setup-pixi@v0
    
    - run: pixi run test
    - run: pixi run py-test
